# Правила
Хотя это пет проект, но качество должно кода должно быть хорошее. 
Проект должен быть четко структурирован. Никакого говнокода. Максимально расширяемо, поддерживаемо, качество кода на очень высоком уровне должно быть.
Запрещено делать try except которые замалчивают ошибку. Также избыточные try except. 
Запрещено делать if None проверки там, где не может быть None. 

Логика разделена четко на 3 уровня (в тч в организации кода, разбито по папкам). 
* Логика работы с базой данных (repositories)
* Бизнес логика (services)
* Эндпоинты (api)
А также модели данных.  (models)

Обильное логирование делаем. 
Логгирование через логгер. 

Максимально возможное покрытие тестами. Не скупимся на реальные запросы в LLM в тестах. LLM локальная. 

Стек - fastAPI python, SQLite, pytest
Регистрации/логина нет - один пользователь. Логику пользователей не делать. 
Ключи для LLM хранятся в .env файле. 
# MarketResearch 
"папка" запроса, главный объект

хранит состояние. является конечным автоматом. 
используем паттерн state, отдельный метод для перехода в следующее состояние. 
Состояния:
* CHAT - ждем сообщение пользователя, получаем, генерируем ответ. В ответе можем встретить 2 типа вызова инструментов - quick_research, deep_research. в зависимости от этого переходим в другие состояния. 
* SEARCHING_QUICK - быстрый поиск, передает запрос в расширение, получает результаты, закидывает их в историю чата, пишется ответ. отсюда переходим в CHAT 
* PLANNING_DEEP_RESEARCH - когда модель решает что ей нужно начать глубокое исследование, то сначала параметры нужно обсудить с человеком. можно перейти либо в CHAT если отмена, либо в DEEP_RESEARCH, либо остаться в PLANNING_DEEP_RESEARCH. Это все еще чат. Решение о переходе в другое состояние принимает LLM. То есть LLM предлагает параметры глубокого исследования, спрашивает человека подойдут ли они. Человек пишет валяй, LLM делает tool call. (не if else на строках!)
* DEEP_RESEARCH - парсинг многих объявление, структурирование их по схеме. Фильтрация, турнир. Отсюда в CHAT
### история чата
тут хранится история чата человека и агента. в истории чата также видны вызовы инструментов, результаты исследований, общения с субагентами(глубокое исследование), найденные лоты превью и ссылки.

Человек может присылать изображения. 

Под сообщениями в которых выполнялся поиск, или есть отсылки на результаты поиска - могут быть плитки с найденными лотами. 
	Если это deep research плитка, то должна быть кнопка "треугольничка" для просмотра заполненной схемы. 

Если история становится слишком длинной, использовать пересказ для сжатия. 
Длину истории считаем в токенах. (должно быть в объекте ответа от openai сервера если не ошибаюсь, prompt tokens)
## Поисковая задача
в одном market research может быть несколько поисковых задач по ходу чата. 

режим - quick, deep
У обоих режимов Есть флаг needs_visual - bool. Для пк компонентов нам например неинтересно на них смотреть. А если мебель и одежда то нужно. 

##### quick 
- без схемы. расширение присылает текст лотов, цены. просто пишем ответ по найденным лотам. 
##### deep
- с json схемой (схема согласованная с пользователем), также кратеньким описанием ситуации, что нужно пользователю, что он ищет. (для того чтобы субагенту было проще извлекать параметры и проводить турнир.)
	json схему согласуем с пользователем.  
	Турнирный реранкинг:
	Нужен для определения лучших товаров из многих. Обработанные товары разбиваются на группы по 5 штук, с перекрытием. 
	Каждая группа обрабатывается отдельно. 
	Для каждой группы просим LLM отранжировать товары от 1 до 5 от лучшего к худшему. 
	Вычисляем итоговые рейтинги по Borda - каждому товару начисляются баллы в зависимости от его места в группе. Чем выше, тем больше баллов. (аккуратно - не должно быть ошибок. Если товар участвует чаще других в соревнованиях, это не должно давать ему больше баллов)
	После обработки всех групп, товары сортируются по набранным баллам по убыванию. 

Дублирования кода между quick и deep не должно быть, но также не должно быть ужасных if else. Тут стоит использовать ООП паттерны. 

### Схема 
Задумка в том, что пользователь говорит агенту что его интересует в товаре в первую очредь. и агент когда исследует рынок, выписывает это четко и формально, и затем по этому можно делать сортировку. 

Имеет 
* название схемы, 
* описание (что мы ищем, небольшой контекст диалога)
* json модель, которую нужно заполнить. 
Должна быть возможность конвертировать json в pydantic для structured outputs.
Хранится в бд. несколько поисковых задач могут быть запущены по одной схеме. Например, в рамках одного чата. 


### Сырой лот
То, что нам нашло расширение в браузере. 
(если сырой лот попадается дважды один и тот же (url), но он изменился, надо его обновить. )
Изображения хранится в папке в проекте. 
data/images/{hash}.{ext} 
при скачивании не сохранять дубли. 

### Проанализированный лот
ссылка на сырой лот
ссылка на схему
структурированные данные - json по схеме. 
Кроме схемы, всегда обязательно visual_notes - то что видно на картинке, но не указано в тексте + небольшое описание картинки. 


---

# Требования

Общение бота и человека должно происходить бесшовно. 
Человек наприсал привет почем gtx4060ti? 
агент понимает что это такой поверхностный пристрелочный запрос, в quick режиме идет искать. 
немного результатов собирает, показывает, рассказывает. 
человек говорит ого круто я хочу. у меня 50к есть, мне нужно с 3 вентиляторами, не после майнинга. 
агент понимает что это уже глубокий запрос с требованиями, формулирует схему. 
* fans_count
* mining mentioned
Также кратенько описывает ситуацию и контекст диалога. 
Человек подтверждает схему. 
Схема уходит в поиск. 
Набираются данные по этой схеме. 
Агент может поставить фильтры если результатов много. 
Если результатов все еще много то турнирный реранкинг. 

То есть это всегда чат. Решение о том, нужно ли запускать поиск - принимает чат агент. Параметры поиска - заполняет чат агент. Определяет подтвердил ли человек схему для поиска - все тот же чат агент. 

---

# Браузерное расширение. 
Является клиентом. Постоянно опрашивает сервер, как там задачи спрашивает. 
Уже реализовано, не трогать. 
Слушает на 8001 порту. 
## **📡 API контракты**

### **1. Получение задачи от сервера**

**Endpoint:** `GET /api/get_task`

**Ответ сервера (200 OK):**


```
{
  "task_id": 12345,
  "query": "ноутбук asus",
  "active_tab": true,
  "limit": 50
}
```

**Поля:**

- `task_id` (обязательный) - уникальный ID задачи для отслеживания
- `query` (обязательный) - поисковый запрос для Avito
- `active_tab` (опциональный, default: `true`) - открывать ли вкладку активной
- `limit` (опциональный, default: 100) - лимит товаров для сбора (динамический параметр)

 ### **2. Отправка результатов на сервер**

**Endpoint:** `POST /api/submit_results`

**Тело запроса:**

```
{
  "task_id": 12345,
  "items": [
    {
      "title": "Ноутбук ASUS TUF Gaming",
      "price": "45000",
      "url": "https://www.avito.ru/...",
      "description": "Игровой ноутбук...",
      "image_base64": "data:image/jpeg;base64,/9j/4AAQSkZJRgABAQ..."
    }
  ]
}
```


**Структура объекта товара:**

```
interface AvitoItem {
  title: string;          // Название товара
  price: string;          // Цена (строка, может содержать пробелы)
  url: string;            // Полная ссылка на товар
  description: string;    // Описание товара (может быть пустым)
  image_base64: string | null; // Base64 изображения или null
}
```

